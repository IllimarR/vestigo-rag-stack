"""Retrieval service — semantic search over the vector store.

Retrieval has no contract of its own; it composes `EmbeddingProvider`,
`VectorStoreRepository`, and `Reranker` to serve search. Phase 1 holds the
directory as a placeholder for Phase 3's implementation.
"""
